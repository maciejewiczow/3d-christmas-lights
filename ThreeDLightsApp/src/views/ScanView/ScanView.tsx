import React from 'react';
import { Text } from 'react-native';
import { ScanViewWrapper } from './parts';

export const ScanView: React.FC = () => (
    <ScanViewWrapper>
        <Text>ScanView</Text>
    </ScanViewWrapper>
);
