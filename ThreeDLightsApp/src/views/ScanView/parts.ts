import styled from 'styled-components/native';
import { View } from '~/src/components/themed';

export const ScanViewWrapper = styled(View)`
    flex: 1;
    justify-content: center;
    align-items: center;
`;
